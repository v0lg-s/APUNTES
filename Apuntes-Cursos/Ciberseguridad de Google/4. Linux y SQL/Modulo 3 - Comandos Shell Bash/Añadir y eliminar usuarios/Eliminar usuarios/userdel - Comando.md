El comando userdel elimina un usuario al sistema. 
- Solo el usuario Root o usuarios con privilegios SUDO pueden ejecutar este comando.
- Hay que tener precaución al borrar un usuario con este comando.

```bash
sudo userdel nuevoUsuario7
```

**OPCIONES**
- **-r:** Borra el usuario y sus archivos