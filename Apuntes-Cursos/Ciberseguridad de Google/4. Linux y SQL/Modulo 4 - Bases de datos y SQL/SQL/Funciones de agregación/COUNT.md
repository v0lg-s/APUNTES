Devuelve un único número que representa el número de filas devueltas por la consulta.

```SQL
SELECT COUNT(firstname)
FROM customers
WHERE country = 'USA';
```

Si desea encontrar el número de clientes de un país concreto.