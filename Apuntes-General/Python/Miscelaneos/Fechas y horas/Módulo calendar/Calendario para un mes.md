
La función llamada `month` del módulo calendar, permite mostrar un calendario para un mes especifico.

```python
import calendar
print(calendar.month(2020,11))
```

También está la función `prmonth`.

