El modulo `os` tiene un método que devuelve información del directorio de trabajo actual. Es `getcwd()`.

```python
import os
print(os.getcwd)
```

va a mostrar la ruta absoluta hasta donde se ejecutó el archivo.