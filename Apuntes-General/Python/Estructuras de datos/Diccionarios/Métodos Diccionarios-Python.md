Entre otros, existen los siguientes métodos:
- **`diccionario.keys()`**:  Devuelve una lista con las claves.
- **`diccionario.values()`**:  Devuelve una lista con los valores.
- **`diccionario.items()`**:  Devuelve una lista de tuplas con los pares de clave-valor.
- **`diccionario.pop(key)`**: Elimina la clave especificada y su valor. 