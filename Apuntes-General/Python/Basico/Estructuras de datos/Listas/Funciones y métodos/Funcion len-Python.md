La función len() toma como argumento el nombre de una lista y devuelve la cantidad de elementos que esta contiene.

```python
numbers = [10, 5, 7, 8, 9]
print("Longitud de la lista:", len(numbers))
```

