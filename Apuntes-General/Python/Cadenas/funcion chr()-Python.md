Devuelve el carácter a partir del punto de código (número).

Por ejemplo, en la codificación UNICODE el carácter *'a'* tiene como punto de código el número *97*

```python
print(chr(97)) # muestra el carácter 'a'

print(chr(945)) # muestra 'α' alfa
```