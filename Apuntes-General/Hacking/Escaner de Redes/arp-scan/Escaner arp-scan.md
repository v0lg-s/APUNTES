Este escaner de red está especializado en el uso de consultas ARP.

Una de las opciones más populares es:

```bash
arp-scan --localnet
arp-scan -l
```

Este escanea todos los posibles hosts/IP's válidas dentro de la red local.

Podemos ver mas opciones buscando su manual.