## Comando mkdir
*mkdir* nos permite crear directorios.

- mkdir → Make Directory

```bash
mkdir carpeta_nueva
```

- mkdir -p $\rightarrow$ crea directorios recursivamente.

```bash
mkdir -p directorio1/directorio2
```

Crea todos los directorios que se especifiquen en la ruta. 
`directorio1` contiene ``directorio2``