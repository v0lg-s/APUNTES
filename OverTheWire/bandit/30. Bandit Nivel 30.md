La contraseña está en un repositorio de git en `ssh://bandit29-git@localhost/home/bandit29-git/repo` en el puerto 2220 la contraseña para el usuario *bandit29-git* es la misma que la del usuario *bandit29*

Para clonar este repositorio se usa:

```bash
git clone ssh://bandit29-git@localhost:2220/home/bandit29-git/repo
```

Se clona el repositorio `repo` con un archivo llamado `README.md` cuyo contenido es el siguiente:

```plaintext
# Bandit Notes
Some notes for bandit30 of bandit.

## credentials

- username: bandit30
- password: <no passwords in production!>
```

Tras revisar en el repositorio clonado no encuentro ningún otro archivo. Dado que es un repositorio es probable que haya habido cambios o *commits* que se puedan recuperar. En los commits de la rama actual no hay nada. 

Pero revisando las ramas hay una en especifico llamado *dev* de *development*, moviéndome a esa rama:

```bash
git checkout dev
```

Y revisando el contenido del archivo `README.md`, ahora la contraseña está en el archivo.

**Password for bandit30:**
qp30ex3VLz5MDG1n91YowTv4Q8l7CDZL