En el directorio principal de *bandit 19* hay un archivo binario ejecutable con el bit *[[SetUID]]* activo.

El dueño de este archivo es *bandit 20*, por lo que ejecutar este binario provocara que los procesos que desencadene el ejecutable se ejecuten con los *privilegios de bandit 20.*

Este binario en particular permite que le pasemos como argumento un comando de bash y lo ejecutará.

**RECORDAR:** Las contraseñas de los niveles están en la ruta `/etc/bandit_pass/bandit#`.

```bash
./bandit20-do cat /etc/bandit_pass/bandit20
```

**Password for bandit 20:**
0qXahG8ZjOVMN9Ghs7iOWsCfZyXOUbYO