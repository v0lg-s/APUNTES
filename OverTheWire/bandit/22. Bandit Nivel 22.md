Se está corriendo un trabajo [[Tareas Cron|cron]] en el servidor. Nos dicen que podemos revisar el directorio `/etc/cron.d.` para encontrar el archivo de configuración para la tarea.

Listé el contenido del directorio:

```bash
ls /etc/cron.d
```

En este hay varias tareas programadas con nombres de distintos usuarios. Entre ellas `cronjob_bandit22`, cuyo contenido es el siguiente:

```plaintext
@reboot bandit22 /usr/bin/cronjob_bandit22.sh &> /dev/null           
* * * * * bandit22 /usr/bin/cronjob_bandit22.sh &> /dev/null 
```

Es el archivo en el que se configura una tarea para que se ejecute:
1. Cada vez que se inicia el sistema.
2. Cada minuto de cada hora, todos los días.

Podemos ver la ruta del script: `/usr/bin/cronjob_bandit22.sh`

Quiero ver el funcionamiento de este script por lo que hago un *cat* a esta ruta.

```bash
cat /usr/bin/cronjob_bandit22.sh
```

La salida es:

```bash
#!/bin/bash                      
chmod 644 /tmp/t7O6lds9S0RqQh9aMcz6ShpAoZKF7fgv                      
cat /etc/bandit_pass/bandit22 > /tmp/t7O6lds9S0RqQh9aMcz6ShpAoZKF7fgv 
```

- Básicamente este script, cambia los permisos de un archivo en el directorio temporal a 644, y posteriormente copia la contraseña de *bandit 22* a dicho archivo.

Encontramos la ubicación de la contraseña por lo que basta con revisar el contenido del archivo (Sé que lo puedo ver porque los permisos '644' otorgan permiso de lectura a el *grupo* y *otros* ):

```bash
cat /tmp/t7O6lds9S0RqQh9aMcz6ShpAoZKF7fgv 
```

**Password for bandit 22:**
tRae0UfB9v0UzbCdn9cY0gQnds9GF58Q 