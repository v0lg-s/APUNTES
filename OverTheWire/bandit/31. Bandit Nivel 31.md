La contraseña está en un repositorio de git en `ssh://bandit30-git@localhost/home/bandit30-git/repo` en el puerto 2220 la contraseña para el usuario *bandit30-git* es la misma que la del usuario *bandit30*.

Hay un archivo llamado `README.md` en el directorio del repositorio, cuyo contenido es:

```plaintext
just an empty file... muahaha
```

A diferencia de los niveles anteriores aquí no hay diferentes ramas ni múltiples commits.

Revisando los tags con;

```bash
git tag -l
```

Vemos que hay un tag llamado `secret`.

El comando que nos permite movernos al commit al que este tag hace referencia es:

```bash
git checkout tags/nombreTag
```

En este caso, recibimos un error que dice que la referencia `tags/secret` no es un arbol. Ahora, procedo a verificar el tag `secret`.

```bash
git tag -v secret
```

La respuesta es : `error: secret: cannot verify a non-tag object of type blob.` Esto quiere decir que el tag `secret` no está haciendo referencia a un commit.

Mostramos el contenido del tag o en este caso del tipo 'blob'.

```bash
git show secret
```

La respuesta es la contraseña

**Password for bandit 31:**
fb5S2xb7bRyFmAvQYQGEqsbhVyJqhnDy