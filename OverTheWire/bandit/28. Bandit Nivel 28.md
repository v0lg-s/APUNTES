La contraseña está en un repositorio de git en `ssh://bandit27-git@localhost/home/bandit27-git/repo` en el puerto 2220 la contraseña para el usuario *bandit27-git* es la misma que la del usuario *bandit27*

Para clonar este repositorio se usa:

```bash
git clone ssh://bandit27-git@localhost:2220/home/bandit27-git/repo
```

Teniendo en cuenta la estructura de las URL's de git:

```plaintext
ssh://[<user>@]<host>[:<port>]/<path-to-git-repo>

git://<host>[:<port>]/<path-to-git-repo>

http[s]://<host>[:<port>]/<path-to-git-repo>

ftp[s]://<host>[:<port>]/<path-to-git-repo>
```

Leemos el archivo README.txt

**Password for bandit 28:**
Yz9IpL0sBcCeuG7m9uQFt8ZNpS4HZRcN

